# Generated by Move
# This file contains metadata about published versions of this package in different environments
# This file SHOULD be committed to source control
#
# `deploy_sui` is NOT YET PUBLISHED to testnet. Publishing is gated + owner-approved
# (docs/deploy/SPEC.md §13). On publish, the CLI fills in `[published.testnet]` with
# the real package id + upgrade-cap; the published package id, the shared `Version`
# object id, and the shared `DomainRegistry` object id then get written into
# @suize/shared (PACKAGE_IDS.DEPLOY, currently '0x0' placeholders).
