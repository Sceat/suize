node_modules
dist
tsconfig.tsbuildinfo
.env
.env.local
*.local
.DS_Store
bun.lock
bun.lockb
.movesrc

# Move build artifacts (compiled bytecode — regenerated by `sui move build`)
move/build/

# Screenshots / scratch images (never commit)
*.png
# …except the OG share card — a versioned brand asset.
!public/og.png
.playwright-mcp/
.vercel
